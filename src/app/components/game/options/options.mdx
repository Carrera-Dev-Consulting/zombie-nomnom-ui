import {Meta, Primary, Controls, Story} from '@storybook/blocks';
import * as OptionsStories from './options.stories';

<Meta of={OptionsStories} />

# Options

A Options is a component that can be used to render a list of game options on the page.

<Primary />

## Props 

<Controls />

## Stories

### Basic Options

This is what a basic options looks like when redered on the page

<Story of={OptionsStories.BasicOptions} />