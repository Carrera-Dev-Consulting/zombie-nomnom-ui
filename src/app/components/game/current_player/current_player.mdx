import {Meta, Primary, Controls, Story} from '@storybook/blocks';
import * as CurrentPlayerStories from './current_player.stories';

<Meta of={CurrentPlayerStories} />

# Current Player

A Current Player is a component that can be used to render a current player on the page.
<Primary />

## Props 

<Controls />

## Stories

### Basic 

This is what a basic current player looks like when redered on the page
<Story of={CurrentPlayerStories.BasicCurrentPlayer} />